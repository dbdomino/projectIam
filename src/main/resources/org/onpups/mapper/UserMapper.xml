<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.onpups.mapper.UserMapper">
	<insert id="create">
		insert into table_user (id, email, password)
		values (#{cumd1}, #{cumd2}, #{numd1})
	</insert>
	
	<select id="read" resultType="org.onpups.domain.TableUserVO">
		select num, id, email, regDate, password from table_user where id = #{id}
	</select>
	
	<update id="update">
		update tbl_board set
		 title= #{title},
		 content=#{content},
		 writer=#{writer},
		 upregdate=sysdate
		 where bno=#{bno}
	</update>
	<update id="delete">
		update tbl_board set
		 title= #{title},
		 content=#{content},
		 writer=#{writer},
		 upregdate=sysdate
		 where bno=#{bno}
	</update>
	
	
	
</mapper>